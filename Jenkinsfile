pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                script {
                    sh 'docker run hello-world'
                }
            }
        }
    }
}
